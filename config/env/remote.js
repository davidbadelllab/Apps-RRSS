/*
  These are the environment settings for the TEST environment.
  This is the environment run with `npm start` if EXPRESS_ENV=test.
  This is the environment run by the test suite.
*/

module.exports = {
  refesh_token: 'secret-boilerplate-token',
  token: 'secret-jwt-token',
  database: '',
  env: 'test'
};
